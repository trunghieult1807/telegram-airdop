services:
  bot:
    build: .
    volumes:
      - .:/app
    command: python3 main.py
    tty: true
    stdin_open: true
    develop:
      watch:
        - action: sync
          path: .
          target: /app
